<?xml version="1.0" encoding="utf-8"?>
<!--
  Yalnızca debug derlemesi için.

  Üretimde tüm trafik HTTPS'tir (vaktinde.devpos.com). Bu istisna sadece
  geliştirme sırasında emülatörün ana makinedeki API'ye (10.0.2.2) düz HTTP
  ile ulaşabilmesi içindir. Sürüm derlemesinde bu dosya bulunmaz, dolayısıyla
  Android'in varsayılan "düz metin trafiği yasak" kuralı geçerli kalır.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
